# 番茄土豆 v9.1 功能测试指南

## 🧪 测试清单

### ✅ 测试1：今日番茄记录标签前置显示

**测试步骤**：
1. 添加一个带标签的任务：`写代码 #工作 #重要`
2. 开始番茄钟并完成
3. 保存记录
4. 查看"今日番茄记录"卡片

**预期结果**：
- ✅ 显示格式：`10:00-10:25 #工作 #重要 写代码`
- ✅ 标签在描述之前
- ✅ 标签之间有适当间距
- ✅ 时间显示正常

**测试截图位置**：
- 右侧"今日番茄记录"卡片

---

### ✅ 测试2：番茄历史记录标签前置显示

**测试步骤**：
1. 完成多个带标签的番茄钟
2. 点击底部"番茄历史"标签
3. 查看历史记录列表

**预期结果**：
- ✅ 显示格式：`10:00-10:25 #工作 #重要 写代码`
- ✅ 标签在描述之前
- ✅ 标签不会被压缩（flex-shrink-0）
- ✅ 任务描述可以正常截断（truncate）
- ✅ 按日期分组显示正常

**测试场景**：
- 短任务名称：`#工作 开会`
- 长任务名称：`#工作 #重要 #紧急 完成一个非常长的任务描述测试截断效果`
- 无标签任务：`写代码`

---

### ✅ 测试3：任务历史记录标签前置显示

**测试步骤**：
1. 完成几个带标签的任务
2. 点击底部"任务历史"标签
3. 查看已完成任务列表

**预期结果**：
- ✅ 显示格式：`☑ #工作 #重要 写代码 完成3个番茄`
- ✅ 标签在描述之前
- ✅ 标签使用outline样式
- ✅ 任务描述有删除线
- ✅ 完成番茄数显示在最右侧

**测试场景**：
- 多标签任务
- 单标签任务
- 无标签任务

---

### ✅ 测试4：浏览器通知功能

#### 4.1 权限请求测试

**测试步骤**：
1. 清除浏览器数据（或使用隐身模式）
2. 访问番茄土豆应用
3. 观察是否弹出通知权限请求

**预期结果**：
- ✅ 自动弹出权限请求对话框
- ✅ 对话框内容清晰
- ✅ 可以选择"允许"或"阻止"

#### 4.2 通知发送测试

**测试步骤**：
1. 确保已授予通知权限
2. 开始一个番茄钟（可以设置为1分钟测试）
3. 等待番茄钟完成
4. 观察是否收到通知

**预期结果**：
- ✅ 番茄完成时立即弹出通知
- ✅ 通知标题：`🍅 番茄钟完成！`
- ✅ 通知内容：`恭喜完成 25 分钟的专注时间：任务名称`
- ✅ 通知图标显示正常
- ✅ 3秒后自动关闭

#### 4.3 通知交互测试

**测试步骤**：
1. 完成番茄钟，收到通知
2. 切换到其他标签页
3. 点击通知

**预期结果**：
- ✅ 点击通知后聚焦到番茄土豆标签页
- ✅ 通知立即关闭
- ✅ 应用正常显示

#### 4.4 后台通知测试

**测试步骤**：
1. 开始番茄钟
2. 切换到其他标签页或最小化浏览器
3. 等待番茄完成

**预期结果**：
- ✅ 即使在后台也能收到通知
- ✅ 通知内容正确
- ✅ 点击通知能正确聚焦

#### 4.5 权限被拒绝测试

**测试步骤**：
1. 在浏览器设置中拒绝通知权限
2. 完成一个番茄钟

**预期结果**：
- ✅ 不会弹出通知
- ✅ 应用其他功能正常
- ✅ 控制台无错误（或仅有权限提示）

---

## 🔍 边界情况测试

### 测试5：无标签任务

**测试步骤**：
1. 添加无标签任务：`写代码`
2. 完成番茄钟
3. 查看各个历史记录区域

**预期结果**：
- ✅ 今日记录：`10:00-10:25 写代码`（ml-2间距）
- ✅ 番茄历史：`10:00-10:25 写代码`（ml-2间距）
- ✅ 任务历史：`☑ 写代码 完成1个番茄`

### 测试6：多标签任务

**测试步骤**：
1. 添加多标签任务：`写代码 #工作 #重要 #紧急 #前端`
2. 完成番茄钟
3. 查看显示效果

**预期结果**：
- ✅ 所有标签都显示
- ✅ 标签之间有适当间距
- ✅ 标签不会换行（或合理换行）
- ✅ 任务描述正常显示

### 测试7：长任务名称

**测试步骤**：
1. 添加长任务：`#工作 这是一个非常非常非常非常非常非常长的任务描述用来测试截断效果`
2. 完成番茄钟
3. 查看各个区域的显示

**预期结果**：
- ✅ 标签完整显示
- ✅ 任务描述被截断（...）
- ✅ 布局不会错乱
- ✅ hover可以看到完整内容（如果实现了tooltip）

---

## 🌐 浏览器兼容性测试

### 测试8：Chrome浏览器

**测试环境**：Chrome 120+

**测试项目**：
- [ ] 标签前置显示正常
- [ ] 通知权限请求正常
- [ ] 通知发送正常
- [ ] 通知交互正常
- [ ] 后台通知正常

### 测试9：Firefox浏览器

**测试环境**：Firefox 120+

**测试项目**：
- [ ] 标签前置显示正常
- [ ] 通知权限请求正常
- [ ] 通知发送正常
- [ ] 通知交互正常
- [ ] 后台通知正常

### 测试10：Edge浏览器

**测试环境**：Edge 120+

**测试项目**：
- [ ] 标签前置显示正常
- [ ] 通知权限请求正常
- [ ] 通知发送正常
- [ ] 通知交互正常
- [ ] 后台通知正常

### 测试11：Safari浏览器

**测试环境**：Safari 17+

**测试项目**：
- [ ] 标签前置显示正常
- [ ] 通知权限请求正常（桌面版）
- [ ] 通知发送正常（桌面版）
- [ ] 移动版通知不可用（预期行为）

---

## 📱 移动端测试

### 测试12：移动端标签显示

**测试步骤**：
1. 在移动设备或模拟器上打开应用
2. 完成番茄钟
3. 查看各个历史记录区域

**预期结果**：
- ✅ 标签前置显示正常
- ✅ 在小屏幕上布局合理
- ✅ 标签不会被压缩
- ✅ 任务描述适当截断

### 测试13：移动端通知

**测试步骤**：
1. 在支持通知的移动浏览器上测试
2. 完成番茄钟

**预期结果**：
- ✅ Chrome Android：通知正常
- ✅ Firefox Android：通知正常
- ⚠️ Safari iOS：通知不可用（预期）

---

## 🐛 问题排查

### 通知不显示？

**检查清单**：
1. [ ] 浏览器是否支持通知
2. [ ] 是否授予了通知权限
3. [ ] 操作系统通知是否开启
4. [ ] 浏览器是否在勿扰模式
5. [ ] 是否使用HTTPS（部分浏览器要求）
6. [ ] 控制台是否有错误信息

**调试方法**：
```javascript
// 在控制台执行
console.log('Notification' in window); // 应该返回true
console.log(Notification.permission); // 应该返回'granted'

// 测试发送通知
new Notification('测试', { body: '这是一条测试通知' });
```

### 标签显示不正确？

**检查清单**：
1. [ ] 清除浏览器缓存
2. [ ] 刷新页面
3. [ ] 检查任务数据格式
4. [ ] 查看控制台错误

**调试方法**：
```javascript
// 在控制台执行
const tasks = JSON.parse(localStorage.getItem('pomodoro-tasks') || '[]');
console.log(tasks);

const records = JSON.parse(localStorage.getItem('pomodoro-records') || '[]');
console.log(records);
```

---

## 📊 测试报告模板

```markdown
## v9.1 功能测试报告

**测试日期**: 2025-12-23
**测试人员**: [姓名]
**测试环境**: 
- 浏览器: Chrome 120
- 操作系统: Windows 11
- 设备: Desktop

### 测试结果

| 测试项 | 状态 | 备注 |
|--------|------|------|
| 今日番茄记录标签前置 | ✅ | 显示正常 |
| 番茄历史记录标签前置 | ✅ | 显示正常 |
| 任务历史记录标签前置 | ✅ | 显示正常 |
| 通知权限请求 | ✅ | 自动弹出 |
| 通知发送 | ✅ | 内容正确 |
| 通知交互 | ✅ | 点击聚焦 |
| 后台通知 | ✅ | 正常接收 |
| 无标签任务 | ✅ | 显示正常 |
| 多标签任务 | ✅ | 显示正常 |
| 长任务名称 | ✅ | 截断正常 |

### 发现的问题

1. [问题描述]
   - 重现步骤：
   - 预期结果：
   - 实际结果：
   - 严重程度：

### 建议

1. [改进建议]
```

---

## 🎯 快速测试脚本

### 自动化测试数据生成

在控制台执行以下代码快速生成测试数据：

```javascript
// 生成测试任务
const testTasks = [
  { title: '写代码', tags: ['工作', '重要'] },
  { title: '开会', tags: ['工作'] },
  { title: '学习React', tags: ['学习', '前端'] },
  { title: '写文档', tags: ['工作', '文档'] },
  { title: '无标签任务', tags: [] },
];

// 生成测试记录
const now = new Date();
const testRecords = testTasks.map((task, index) => ({
  id: Date.now() + index,
  taskTitle: task.title,
  tags: task.tags,
  startTime: new Date(now.getTime() - (index + 1) * 30 * 60 * 1000).toISOString(),
  endTime: new Date(now.getTime() - index * 30 * 60 * 1000).toISOString(),
  duration: 25,
  completed: true,
}));

// 保存到localStorage
localStorage.setItem('pomodoro-records', JSON.stringify(testRecords));

// 刷新页面
location.reload();
```

---

**测试完成后请填写测试报告！**

---

**版本**: v9.1  
**更新日期**: 2025-12-23  
**开发者**: 番茄土豆团队
